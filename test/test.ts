import debug from "debug";
const log = debug(`*`);
import * as t from "../src/index"

describe('keys', function() {
  describe('all exist', function() {
    // pending test below
    it('should return a property for every translation');
    log(t);
  });
});
